#!/usr/bin/env python3
"""\nRelease Enforcement Script - CI/CD Integration\n\nThis script enforces evaluation gate requirements before release.\nIt BLOCKS release if evaluation fails.\n\nNO PASS = NO RELEASE\n"""\n\nimport sys\nimport json\nfrom pathlib import Path\nfrom typing import Optional\nimport argparse\n\nfrom .gates import EvaluationResult, EvaluationGateResult\n\n\nclass ReleaseBlockedException(Exception):\n    \"\"\"Raised when release is blocked by evaluation gate\"\"\"\n    pass\n\n\ndef load_evaluation_result(report_path: Path) -> EvaluationGateResult:\n    \"\"\"\n    Load evaluation gate result from JSON report.\n    \n    Args:\n        report_path: Path to evaluation_report.json\n    \n    Returns:\n        EvaluationGateResult\n    \n    Raises:\n        FileNotFoundError: If report doesn't exist\n        ValueError: If report is invalid\n    \"\"\"\n    if not report_path.exists():\n        raise FileNotFoundError(f\"Evaluation report not found: {report_path}\")\n    \n    with open(report_path, \"r\") as f:\n        report_data = json.load(f)\n    \n    # Extract gate result\n    gate_data = report_data.get(\"gate_result\")\n    if not gate_data:\n        raise ValueError(\"Invalid evaluation report: missing gate_result\")\n    \n    return gate_data\n\n\ndef enforce_release_gate(\n    module_name: str,\n    module_version: str,\n    evaluation_report_path: Optional[Path] = None,\n) -> bool:\n    \"\"\"\n    Enforce evaluation gate for release.\n    \n    Args:\n        module_name: Name of the module\n        module_version: Version being released\n        evaluation_report_path: Optional path to evaluation report\n    \n    Returns:\n        True if release is allowed\n    \n    Raises:\n        ReleaseBlockedException: If release is blocked\n    \"\"\"\n    # Find evaluation report\n    if evaluation_report_path is None:\n        reports_dir = Path(__file__).parent / \"reports\"\n        # Find most recent report for this module/version\n        pattern = f\"evaluation_report_{module_name}_{module_version}_*.json\"\n        reports = sorted(reports_dir.glob(pattern), reverse=True)\n        \n        if not reports:\n            raise ReleaseBlockedException(\n                f\"No evaluation report found for {module_name} {module_version}. \"\n                f\"Run evaluation before attempting release.\"\n            )\n        \n        evaluation_report_path = reports[0]\n    \n    # Load evaluation result\n    try:\n        gate_data = load_evaluation_result(evaluation_report_path)\n    except Exception as e:\n        raise ReleaseBlockedException(\n            f\"Failed to load evaluation report: {e}\"\n        )\n    \n    # Check gate result\n    result = gate_data.get(\"result\")\n    blocks_release = gate_data.get(\"blocks_release\", False)\n    can_release = gate_data.get(\"can_release\", False)\n    \n    if result == \"fail\" or blocks_release:\n        # HARD BLOCK - no override allowed\n        failed_checks = gate_data.get(\"failed_checks\", 0)\n        raise ReleaseBlockedException(\n            f\"Release BLOCKED for {module_name} {module_version}\\n\"\n            f\"Evaluation result: {result}\\n\"\n            f\"Failed checks: {failed_checks}\\n\"\n            f\"Review evaluation report: {evaluation_report_path}\\n\"\n            f\"Fix issues before attempting release.\"\n        )\n    \n    elif result == \"warn\":\n        # WARNING - requires manual override\n        warning_checks = gate_data.get(\"warning_checks\", 0)\n        manual_override = gate_data.get(\"manual_override\", False)\n        \n        if not manual_override:\n            raise ReleaseBlockedException(\n                f\"Release BLOCKED for {module_name} {module_version}\\n\"\n                f\"Evaluation result: {result} (requires manual override)\\n\"\n                f\"Warning checks: {warning_checks}\\n\"\n                f\"Review evaluation report: {evaluation_report_path}\\n\"\n                f\"Obtain manual override approval before release.\"\n            )\n        \n        print(f\"⚠️  WARNING: Release allowed with manual override\")\n        print(f\"   Module: {module_name} {module_version}\")\n        print(f\"   Override by: {gate_data.get('override_by', 'unknown')}\")\n        print(f\"   Override reason: {gate_data.get('override_reason', 'not specified')}\")\n        return True\n    \n    elif result == \"pass\":\n        # PASS - release allowed\n        print(f\"✅ Evaluation PASSED for {module_name} {module_version}\")\n        print(f\"   Passed checks: {gate_data.get('passed_checks', 0)}/{gate_data.get('total_checks', 0)}\")\n        print(f\"   Release allowed\")\n        return True\n    \n    else:\n        raise ReleaseBlockedException(\n            f\"Unknown evaluation result: {result}\"\n        )\n\n\ndef block_docker_build(module_name: str, module_version: str) -> None:\n    \"\"\"\n    Block Docker image build if evaluation fails.\n    \n    Args:\n        module_name: Name of the module\n        module_version: Version being built\n    \n    Raises:\n        ReleaseBlockedException: If evaluation fails\n    \"\"\"\n    try:\n        enforce_release_gate(module_name, module_version)\n    except ReleaseBlockedException as e:\n        print(f\"\\n❌ DOCKER BUILD BLOCKED\\n\", file=sys.stderr)\n        print(str(e), file=sys.stderr)\n        sys.exit(1)\n\n\ndef block_version_tag(module_name: str, module_version: str) -> None:\n    \"\"\"\n    Block version tagging if evaluation fails.\n    \n    Args:\n        module_name: Name of the module\n        module_version: Version being tagged\n    \n    Raises:\n        ReleaseBlockedException: If evaluation fails\n    \"\"\"\n    try:\n        enforce_release_gate(module_name, module_version)\n    except ReleaseBlockedException as e:\n        print(f\"\\n❌ VERSION TAG BLOCKED\\n\", file=sys.stderr)\n        print(str(e), file=sys.stderr)\n        sys.exit(1)\n\n\ndef block_hf_space_update(module_name: str, module_version: str) -> None:\n    \"\"\"\n    Block Hugging Face Space update if evaluation fails.\n    \n    Args:\n        module_name: Name of the module\n        module_version: Version being deployed to HF\n    \n    Raises:\n        ReleaseBlockedException: If evaluation fails\n    \"\"\"\n    try:\n        enforce_release_gate(module_name, module_version)\n    except ReleaseBlockedException as e:\n        print(f\"\\n❌ HUGGING FACE SPACE UPDATE BLOCKED\\n\", file=sys.stderr)\n        print(str(e), file=sys.stderr)\n        print(f\"\\nHF Spaces can only point to evaluated and approved versions.\", file=sys.stderr)\n        sys.exit(1)\n\n\ndef main():\n    \"\"\"CLI entry point for release enforcement\"\"\"\n    parser = argparse.ArgumentParser(\n        description=\"Enforce evaluation gate before release\"\n    )\n    parser.add_argument(\n        \"module_name\",\n        help=\"Name of the module being released\"\n    )\n    parser.add_argument(\n        \"module_version\",\n        help=\"Version being released\"\n    )\n    parser.add_argument(\n        \"--action\",\n        choices=[\"release\", \"docker\", \"tag\", \"hf-space\"],\n        default=\"release\",\n        help=\"Action being performed\"\n    )\n    parser.add_argument(\n        \"--report\",\n        type=Path,\n        help=\"Path to evaluation report (optional)\"\n    )\n    \n    args = parser.parse_args()\n    \n    try:\n        if args.action == \"docker\":\n            block_docker_build(args.module_name, args.module_version)\n        elif args.action == \"tag\":\n            block_version_tag(args.module_name, args.module_version)\n        elif args.action == \"hf-space\":\n            block_hf_space_update(args.module_name, args.module_version)\n        else:\n            enforce_release_gate(\n                args.module_name,\n                args.module_version,\n                args.report\n            )\n        \n        print(f\"\\n✅ Release gate check PASSED\")\n        sys.exit(0)\n    \n    except ReleaseBlockedException as e:\n        print(f\"\\n❌ Release gate check FAILED\\n\", file=sys.stderr)\n        print(str(e), file=sys.stderr)\n        sys.exit(1)\n    \n    except Exception as e:\n        print(f\"\\n❌ Unexpected error\\n\", file=sys.stderr)\n        print(str(e), file=sys.stderr)\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n